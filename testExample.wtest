import destinos.*
import mensajeros.*


// TEST 1 --------------------------------------------------------------------------------------------

describe "testeo de toooodo" {

  test "roberto pesa en total 90kg, puede llevar un paquete que está a pago en un camion acoplado" {
    camion.agregarElCoplado(bicicleta)
    assert.that(roberto.puedeEntregarPaqueteAlDestino(puenteMoron))
  }

}